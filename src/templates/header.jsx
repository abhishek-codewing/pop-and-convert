import { Logo } from "@/icons/logo";

export function Header() {
    return (
        <header className="py-6 px-8 flex items-center justify-between shadow">
            <Logo />
        </header>
    );
}
